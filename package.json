{
  "name": "mobile-access-lib",
  "version": "0.1.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test:sample": "node test-sample.js",
    "report": "node generate-report.js",

    "build": "esbuild index.js --bundle --platform=node --format=esm --outfile=dist/index.js --external:xml2js --external:events --external:fs --external:path"
  },
  "dependencies": {
    "xml2js": "^0.6.2"
  }
}
